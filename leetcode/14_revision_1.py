# 1. Contains Duplicates: [Python]
    # just compare length of original list and set(list)

# 2. Valid Anagrams: [Python]
    # just check if length is not same, they are not anagrams
    # if length is same, check if sorted(original) == sorted(anagram)

# =======================================================================
# Currently above one's using python, later do it using DSA.
# =======================================================================

# 3. Two Sum: [DSA]
    # store value -> index in dict{}, as you need to return indices
    # value is nothing but the diff = target - num

# 4. Valid Palindrome: [Python]
    # Just check if reverse of the string is exactly same (just ignore spaces and case-sensitivity)
    # newStr == newStr[::-1]



